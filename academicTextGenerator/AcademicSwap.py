def akademicTextSwaping(inputName, outputName, swapListFile): #Takes in a text and outputs a text with better akademic wringing style, ex without it's aso)
    # outFile = open(outputName, 'w')
    # inputFile = open(inputName, "r")
    #
    # swapList = swapListGen(swapListFile)
    # lines = inputfile.readlines()
    # for line in lines:
    #     for word in line.split()
    #         newWord = findSwapWord(word)
    #         if(newWord != ""):
    #             print(word + " swapped for: " + newWord)
    #             word = newWord + "*"
    #         outFile.write(word)
    #
    # print("new text written to "+ outputName)

#def findSwapWord(keyWord):
    #

#def swapListGen(swaplistFile):
    #todo generate dictionary from an input files
    #swaplist =
